version: '2'
services:
  web:
    build: .
    ports:
     - "80:80"
    volumes:
     - .:/var/www/html/
    depends_on:
     - postgres
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: demo
      POSTGRES_PASSWORD: mysecretpassword
